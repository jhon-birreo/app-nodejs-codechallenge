import * as provider from '../../src/infrastructure/inject-providers/antifraud.provider';

describe('antifraud.provider', () => {
  it('should be defined', () => {
    expect(provider).toBeDefined();
  });
});
